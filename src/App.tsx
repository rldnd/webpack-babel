import React from "react";

import styled from "styled-components";

const App = (): JSX.Element => {
  return <AppWrapper>되라 얍!</AppWrapper>;
};

const AppWrapper = styled.div`
  width: 100%;
  min-height: 100vh;

  background-color: #eee;
`;

export default App;
